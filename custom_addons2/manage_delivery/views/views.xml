<odoo>
    <data>
        <!-- explicit list view definition -->
        <!--
            <record model="ir.ui.view" id="manage_delivery.list">
              <field name="name">manage_delivery list</field>
              <field name="model">manage_delivery.manage_delivery</field>
              <field name="arch" type="xml">
                <tree>
                  <field name="name"/>
                  <field name="value"/>
                  <field name="value2"/>
                </tree>
              </field>
            </record>
        -->

        <!-- actions opening views on models -->
        <!--
            <record model="ir.actions.act_window" id="manage_delivery.action_window">
              <field name="name">manage_delivery window</field>
              <field name="res_model">manage_delivery.manage_delivery</field>
              <field name="view_mode">tree,form</field>
            </record>
        -->

        <!-- server action to the one above -->
        <!--
            <record model="ir.actions.server" id="manage_delivery.action_server">
              <field name="name">manage_delivery server</field>
              <field name="model_id" ref="model_manage_delivery_manage_delivery"/>
              <field name="state">code</field>
              <field name="code">
                action = {
                  "type": "ir.actions.act_window",
                  "view_mode": "tree,form",
                  "res_model": model._name,
                }
              </field>
            </record>
        -->

        <!-- Top menu item -->
        <!--
            <menuitem name="manage_delivery" id="manage_delivery.menu_root"/>
        -->
        <!-- menu categories -->
        <!--
            <menuitem name="Menu 1" id="manage_delivery.menu_1" parent="manage_delivery.menu_root"/>
            <menuitem name="Menu 2" id="manage_delivery.menu_2" parent="manage_delivery.menu_root"/>
        -->
        <!-- actions -->
        <!--
            <menuitem name="List" id="manage_delivery.menu_1_list" parent="manage_delivery.menu_1"
                      action="manage_delivery.action_window"/>
            <menuitem name="Server to list" id="manage_delivery" parent="manage_delivery.menu_2"
                      action="manage_delivery.action_server"/>
        -->
    </data>
</odoo>